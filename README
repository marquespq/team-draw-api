# Team Draw NodeJs API with Typescript

[![GitHub version](https://badge.fury.io/gh/marquespq%2Fteam-draw-api.svg)](https://badge.fury.io/gh/marquespq%2Fteam-draw-api)

Team Draw is a NodeJs API built with Typescript. It is designed to be used in web applications and services, and provides an easy way to interact with the database and other services.

## Requirements

- npm >=6.0.0
- node >=14.0.0

## Installation

Clone the repository:

    git clone https://github.com/marquespq/team-draw-api

Install dependencies:

    npm install

Build the project:

    npm run build

Start the server:

    npm run dev

Run tests:

    npm run test

Run linting:

    npm run lint

Run prettier:

    npm run prettier
